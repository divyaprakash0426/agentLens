# Advanced

- Use `selectorResolver` config to provide custom resolution.
- Use lifecycle callbacks (`onActionStart`, `onActionError`, `onQueueEmpty`) for telemetry.
- Tune timing with `speedMultiplier`, `typingSpeed`, and queue delays.
- Set `executeActions: false` to run in visualization-only sandbox mode.
