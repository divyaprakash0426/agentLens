# ActionParser API

Parses LLM output into `AgentAction[]`.

## Static methods

- `parse(text, format?)`
- `parseGemini(text)`
- `parseOpenAI(toolCalls)`
- `parseAnthropic(blocks)`
- `validate(value)`
- `getSystemPrompt(format)`
