# SelectorResolver API

Resolves selectors with fallback strategies and retry logic.

## Methods

- `resolve(selector)`
- `resolveAll(selector)`
- `SelectorResolver.getElementCenter(element)`

It supports CSS, XPath, text selectors, fuzzy text, and `data-agentlens` shortcuts.
